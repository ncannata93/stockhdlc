{
  "buildCommand": "npm install -g tailwindcss postcss autoprefixer && npm install && npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  }
}
