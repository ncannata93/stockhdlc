{
  "buildCommand": "npm install --no-save && npm run build",
  "installCommand": "npm install --no-save",
  "framework": "nextjs",
  "regions": ["iad1"],
  "functions": {
    "app/**": {
      "maxDuration": 30
    }
  }
}
